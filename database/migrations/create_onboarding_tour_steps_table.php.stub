<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create(config('infinito-onboarding.table_names.tour_steps', 'onboarding_tour_steps'), function (Blueprint $table): void {
            $table->id();
            $table->foreignId('tour_id')
                ->constrained(config('infinito-onboarding.table_names.tours', 'onboarding_tours'))
                ->cascadeOnDelete();
            $table->unsignedInteger('order')->default(0);
            $table->string('target_type', 20)->default('data_tour');
            $table->string('target')->nullable();
            $table->string('title');
            $table->text('body')->nullable();
            $table->string('placement', 20)->default('auto');
            $table->json('extra')->nullable();
            $table->timestamps();

            $table->index(['tour_id', 'order']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists(config('infinito-onboarding.table_names.tour_steps', 'onboarding_tour_steps'));
    }
};
